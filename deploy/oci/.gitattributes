# These files run on the Linux VM — force LF so they are never checked out with
# CRLF (a CRLF shebang/unit line breaks bash and systemd).
*.sh       text eol=lf
*.service  text eol=lf
*.yml      text eol=lf
.env.example text eol=lf
