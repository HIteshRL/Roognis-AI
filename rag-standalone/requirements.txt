# RAG standalone — component dependencies only (no web/DB/auth stack)
pydantic>=2.9
pydantic-settings>=2.6
structlog>=24

# LLM + Vision (Groq)
groq>=0.13

# Document parsing
pypdf==5.1.0
python-docx==1.1.2
python-pptx==1.0.2
markdown==3.7
beautifulsoup4==4.12.3
pillow==11.0.0

# Chunking / embeddings / vector store
tiktoken>=0.8
fastembed>=0.4
qdrant-client>=1.12

# Optional
redis>=5.2        # only if you wire ResponseCacheService (RagService works with cache=None)
openai>=1.57      # only if EMBEDDING_PROVIDER=openai
